# build output
dist/
.astro/

# dependencies
node_modules/

# secrets — never commit real env files (see .env.example)
.env
.env.*
!.env.example

# os / editor
.DS_Store
*.log
.vscode/

# brainstorming visual companion mockups
.superpowers/

# personal journal source data — not deployed (site reads from Sanity)
AppleJournalEntries/

# import pipeline artifacts
scripts/import/.cache/
scripts/import/manifest.json

# site-image upload artifacts (machine-specific local paths)
scripts/site/.cache/
scripts/site/site-images.json

# Sanity CLI runtime artifacts
.sanity/
